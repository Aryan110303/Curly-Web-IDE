import { profile } from "../assets";
import Button from "./Button";

const Hero = () => {
  return (
    <div className="hero">
      <Button className=" " href="#login">
        sTART NOW!
      </Button>
    </div>
  );
};

export default Hero;
